<%= form_with(model: post, class: "space-y-6") do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation" class="ha-card border border-red-200 bg-red-50/80 px-4 py-3 text-sm text-red-700 dark:border-red-500/30 dark:bg-red-500/10 dark:text-red-200">
      <h2 class="font-semibold"><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul class="mt-2 list-disc space-y-1 pl-5">
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, class: "text-sm font-semibold text-[var(--ha-muted)]" %>
    <%= form.text_field :title, class: "ha-input mt-2" %>
  </div>

  <div>
    <%= form.label :body, class: "text-sm font-semibold text-[var(--ha-muted)]" %>
    <%= form.textarea :body, rows: 6, class: "ha-input mt-2" %>
  </div>

  <div class="flex flex-wrap gap-2">
    <%= form.submit class: "ha-button ha-button-primary" %>
  </div>
<% end %>
