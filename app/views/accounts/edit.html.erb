<div class="space-y-6">
  <div>
    <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[var(--ha-muted)]">Account</p>
    <h1 class="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Edit account</h1>
    <p class="mt-2 text-sm text-[var(--ha-muted)]">Update your name.</p>
  </div>

  <div class="ha-card p-6">
    <%= render "form", user: @user %>
  </div>

  <div class="flex flex-wrap gap-2">
    <%= link_to "Back to account", account_path, class: "ha-button ha-button-secondary" %>
  </div>
</div>
