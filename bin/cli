#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ENTRYPOINT="$SCRIPT_DIR/cli-files/cli.rb"

if [[ ! -f "$ENTRYPOINT" ]]; then
  echo "CLI entrypoint not found at $ENTRYPOINT" >&2
  exit 1
fi

exec ruby "$ENTRYPOINT" "$@"
